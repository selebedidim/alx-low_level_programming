#ifndef LIKE_MACRO_H
#define LIKE_MACRO_H

/**
 * file -function-like macro
 * @x:input value
 * description: header file that computes absolute value
 */

#define ABS(x) ((x) < (0) ? -(1) * (x) : (x))

#endif
